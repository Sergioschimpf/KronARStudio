<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>KronAR Studio | Tecnología que optimiza el tiempo</title>

  <!-- Fuentes: Orbitron (para títulos) + Inter (para texto) -->
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      color: #e0e0e0;
      overflow-x: hidden;
      background: #070012;
      transition: background 0.5s ease, color 0.5s ease;
    }

    h1, h2, h3, .logo {
      font-family: 'Orbitron', sans-serif;
    }

    /* ===== ANIMACIÓN DE FONDO ===== */
    .fondo-animado {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at center, #0a0020 0%, #050010 100%);
      overflow: hidden;
      z-index: -1;
      transition: background 0.5s ease;
    }

    .particula {
      position: absolute;
      width: 2px;
      height: 2px;
      background: rgba(0, 200, 255, 0.7);
      border-radius: 50%;
      animation: mover 15s linear infinite;
    }

    @keyframes mover {
      from { transform: translateY(0) scale(1); opacity: 1; }
      to { transform: translateY(-100vh) scale(0.5); opacity: 0; }
    }

    /* ===== HEADER ===== */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 30px;
      background: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(6px);
      position: fixed;
      width: 100%;
      top: 0;
      left: 0;
      z-index: 10;
      box-sizing: border-box;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      transition: background 0.5s ease, border 0.5s ease;
    }

    .logo {
      font-size: 1.8em;
      font-weight: 700;
      color: #d4af37;
      text-shadow: 0 0 15px #ffd700;
      white-space: nowrap;
      transition: all 0.5s ease;
    }

    .logo.brilando {
      animation: brilloDorado 1.5s ease;
    }

    @keyframes brilloDorado {
      0% { text-shadow: 0 0 15px #ffd700; color: #d4af37; }
      50% { text-shadow: 0 0 35px #fff000, 0 0 60px #ffb300; color: #ffeb80; }
      100% { text-shadow: 0 0 15px #ffd700; color: #d4af37; }
    }

    nav.menu {
      display: flex;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    nav.menu a {
      color: white;
      text-decoration: none;
      margin: 0 12px;
      font-size: 0.95em;
      transition: color 0.3s, text-shadow 0.3s;
    }

    nav.menu a:hover {
      color: #d4af37;
      text-shadow: 0 0 10px #d4af37;
    }

    .menu-toggle {
      display: none;
      font-size: 1.8em;
      color: #d4af37;
      cursor: pointer;
      user-select: none;
    }

    @media (max-width: 700px) {
      .menu-toggle { display: block; }

      nav.menu {
        display: none;
        flex-direction: column;
        width: 100%;
        text-align: left;
        background: rgba(0, 0, 0, 0.85);
        padding: 10px 0;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
      }

      nav.menu a {
        display: block;
        margin: 10px 20px;
      }

      nav.menu.show {
        display: flex;
        animation: fadeIn 0.3s ease;
      }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* ===== CONTENIDO ===== */
    section {
      padding: 120px 20px 60px 20px;
      max-width: 1000px;
      margin: auto;
      text-align: center;
      scroll-margin-top: 100px;
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 1s ease, transform 1s ease;
    }

    section.visible {
      opacity: 1;
      transform: translateY(0);
    }

    h1, h2 {
      color: #d4af37;
      text-shadow: 0 0 10px #d4af37;
    }

    p {
      color: #dcdcdc;
      line-height: 1.7em;
      font-size: 1.05em;
    }

    .hero {
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }

    .slogan {
      font-size: 1.2em;
      color: #00e0ff;
      margin-top: 10px;
      text-shadow: 0 0 10px #00e0ff, 0 0 20px #00aaff;
    }

    .boton {
      display: inline-block;
      margin-top: 20px;
      background: transparent;
      border: 2px solid #00e0ff;
      color: #00e0ff;
      padding: 12px 25px;
      border-radius: 30px;
      text-decoration: none;
      transition: all 0.3s;
      box-shadow: 0 0 15px rgba(0, 224, 255, 0.4);
      font-weight: 500;
    }

    .boton:hover {
      background: #00e0ff;
      color: #000;
      box-shadow: 0 0 25px #00e0ff, 0 0 40px #00aaff;
    }

    /* ===== MODO CLARO ===== */
    body.modo-claro {
      background: #f9f9f9;
      color: #222;
    }

    body.modo-claro .fondo-animado {
      background: radial-gradient(circle at center, #ffffff 0%, #d7e4ff 100%);
    }

    body.modo-claro header {
      background: rgba(255, 255, 255, 0.8);
      border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }

    body.modo-claro h1, body.modo-claro h2, body.modo-claro h3, body.modo-claro .logo {
      color: #333;
      text-shadow: none;
    }

    body.modo-claro p {
      color: #333;
    }

    body.modo-claro .boton {
      border-color: #0078ff;
      color: #0078ff;
      box-shadow: none;
    }

    body.modo-claro .boton:hover {
      background: #0078ff;
      color: #fff;
    }

    body.modo-claro footer {
      background: #eaeaea;
      color: #555;
    }
  </style>
</head>

<body>
  <div class="fondo-animado" id="fondo"></div>

  <header>
    <div class="logo" id="logo">KronAR Studio</div>
    <div class="menu-toggle" id="menu-toggle">☰</div>
    <nav class="menu" id="menu">
      <a href="#inicio">Inicio</a>
      <a href="#quienes">Quiénes somos</a>
      <a href="#servicios">Servicios</a>
      <a href="#contacto">Contacto</a>
    </nav>
    <!-- Botón de modo claro/oscuro -->
    <button id="modo-toggle" style="
      background:none;
      border:2px solid #d4af37;
      color:#d4af37;
      padding:5px 12px;
      border-radius:20px;
      cursor:pointer;
      font-family:'Inter',sans-serif;
      font-size:0.9em;
      transition:all 0.3s;
    ">🌙</button>
  </header>

  <section id="inicio" class="hero">
    <h1>KronAR Studio</h1>
    <p class="slogan">Tecnología que optimiza el tiempo</p>
    <a href="#contacto" class="boton">Contactanos</a>
  </section>

  <section id="quienes">
    <h2>Quiénes somos</h2>
    <p>
      <strong>KronAR Studio</strong> nació con una idea simple: usar tecnología para devolverle tiempo a la gente.  
      No hablamos de magia, sino de procesos que fluyen, tareas que se automatizan y decisiones que se toman con datos, no con suposiciones.
    </p>
    <p>
      Somos un equipo obsesionado con la eficiencia, el diseño limpio y las soluciones que realmente se usan.
      Trabajamos con profesionales, técnicos y empresas que valoran lo simple, lo útil y lo bien hecho.
    </p>
  </section>

  <section id="servicios">
    <h2>Qué hacemos</h2>
    <p>
      Desarrollamos herramientas digitales que eliminan lo innecesario y mejoran lo que ya existe.  
      Si algo se repite, se puede automatizar. Si algo es confuso, se puede rediseñar.
    </p>
    <div style="display:flex; flex-wrap:wrap; justify-content:center; gap:20px; margin-top:30px;">
      <div style="background:rgba(255,255,255,0.05); border:1px solid rgba(255,255,255,0.1); border-radius:12px; padding:20px; width:280px;">
        <h3>🌐 Sitios que funcionan</h3>
        <p>Webs rápidas, estéticas y sin adornos innecesarios. Pensadas para mostrar, vender o informar sin hacer perder tiempo.</p>
      </div>
      <div style="background:rgba(255,255,255,0.05); border:1px solid rgba(255,255,255,0.1); border-radius:12px; padding:20px; width:280px;">
        <h3>⚙️ Automatizaciones</h3>
        <p>Conectamos tus flujos de trabajo con herramientas que ahorran horas. Lo que antes tomaba un día, ahora pasa en segundos.</p>
      </div>
      <div style="background:rgba(255,255,255,0.05); border:1px solid rgba(255,255,255,0.1); border-radius:12px; padding:20px; width:280px;">
        <h3>📊 Formularios inteligentes</h3>
        <p>Recolectá datos sin dolor. Formularios que se integran con tus sistemas y muestran resultados en tiempo real.</p>
      </div>
    </div>
  </section>

  <section id="contacto">
    <h2>Contacto</h2>
    <p>¿Tenés un proyecto en mente o querés mejorar tus procesos digitales?</p>
    <a href="https://wa.me/5493515721199" class="boton" target="_blank">📞 Escribinos por WhatsApp</a>
    <p style="margin-top:20px;">o escribinos a <a href="mailto:kronarstudio@gmail.com" style="color:#00e0ff; text-decoration:none;">kronarstudio@gmail.com</a></p>
  </section>

  <footer style="background:#000; padding:20px; text-align:center; color:#888;">
    © 2025 KronAR Studio — Tecnología que optimiza el tiempo
  </footer>

  <script>
    const toggle = document.getElementById('menu-toggle');
    const menu = document.getElementById('menu');
    const logo = document.getElementById('logo');

    toggle.addEventListener('click', () => {
      menu.classList.toggle('show');
      logo.classList.add('brilando');
      setTimeout(() => logo.classList.remove('brilando'), 1500);
    });

    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        document.querySelector(this.getAttribute('href')).scrollIntoView({ behavior: 'smooth' });
        menu.classList.remove('show');
      });
    });

    const fondo = document.getElementById('fondo');
    for (let i = 0; i < 60; i++) {
      const p = document.createElement('div');
      p.classList.add('particula');
      p.style.left = Math.random() * 100 + 'vw';
      p.style.top = Math.random() * 100 + 'vh';
      p.style.animationDuration = (10 + Math.random() * 20) + 's';
      p.style.animationDelay = Math.random() * 15 + 's';
      fondo.appendChild(p);
    }

    const secciones = document.querySelectorAll("section");
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add("visible");
      });
    }, { threshold: 0.2 });

    secciones.forEach(sec => observer.observe(sec));

    // ===== MODO CLARO/OSCURO =====
    const btnModo = document.getElementById("modo-toggle");
    let modoOscuro = true;

    btnModo.addEventListener("click", () => {
      document.body.classList.toggle("modo-claro");
      modoOscuro = !modoOscuro;
      btnModo.textContent = modoOscuro ? "🌙" : "☀️";
    });
  </script>
</body>
</html>
